node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
.env*
!.env.example
.DS_Store

# Planilhas de origem: contêm dados reais de clientes (nome, consumo, consultor).
# O repositório é público — os números já estão transcritos e documentados em
# DOCUMENTACAO-REGRAS-PLANILHA.md, então o arquivo não precisa ser versionado.
*.xlsx
*.xls
